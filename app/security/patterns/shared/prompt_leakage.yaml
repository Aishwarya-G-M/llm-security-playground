category: prompt_leakage
patterns:
  - id: system_prompt_extraction
    pattern: "reveal\\s+(your\\s+)?system\\s+prompt"
    description: "Attempts to extract the system prompt"
    severity: 9

  - id: internal_instructions_extraction
    pattern: "show\\s+(me\\s+)?(your\\s+)?internal\\s+instructions"
    description: "Attempts to expose internal instructions"
    severity: 9

  - id: system_message_dump
    pattern: "print\\s+(the\\s+)?full\\s+system\\s+message"
    description: "Attempts to dump full system configuration"
    severity: 9

  - id: hidden_prompt_request
    pattern: "(display|output|show)\\s+(your\\s+)?(initial|hidden|secret)\\s+(prompt|instruction)"
    description: "Attempts to retrieve hidden initialization instructions"
    severity: 9